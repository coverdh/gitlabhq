- remote = local_assigns.fetch(:remote, false)

.snippets-list-holder
  %ul.content-list
    = render partial: 'shared/snippets/snippet', collection: @snippets
    - if @snippets.empty?
      %li
        .nothing-here-block 没有代码片段

  = paginate @snippets, theme: 'gitlab', remote: remote

:javascript
  gl.SnippetsList();
