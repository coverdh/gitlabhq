= form_for @project_member, as: :project_member, url: namespace_project_project_members_path(@project.namespace, @project), html: { class: 'form-horizontal users-project-form' } do |f|
  .form-group
    = f.label :user_ids, "用户", class: 'control-label'
    .col-sm-10
      = users_select_tag(:user_ids, multiple: true, class: 'input-large', scope: :all, email_user: true)
      .help-block
        可以通过名字、用户名、邮箱查找用户，你也可以通过邮箱邀请新的用户。

  .form-group
    = f.label :access_level, "访问权限", class: 'control-label'
    .col-sm-10
      = select_tag :access_level, options_for_select(ProjectMember.access_level_roles, @project_member.access_level), class: "project-access-select select2"
      .help-block
        了解更多，请查看
        %strong= link_to "权限说明", help_page_path("user/permissions"), class: "vlink"

  .form-group
    = f.label :expires_at, '访问过期时间', class: 'control-label'
    .col-sm-10
      .clearable-input
        = text_field_tag :expires_at, nil, class: 'form-control js-access-expiration-date', placeholder: '选择过期时间'
        %i.clear-icon.js-clear-input
      .help-block
        在这个时间点, 成员会自动失去访问这个项目的权限.

  .form-actions
    = f.submit '添加成员', class: "btn btn-create"
